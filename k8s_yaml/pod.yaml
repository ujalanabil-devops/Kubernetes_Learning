apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    env: demo
    type: frontend
    app: nginx
spec:
  containers:
  - name: nginx-containers
    image: nginx
    ports:
    - containerPort: 80 
    